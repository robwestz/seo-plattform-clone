apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: seo-platform
  labels:
    app: ml-service
    tier: worker
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
