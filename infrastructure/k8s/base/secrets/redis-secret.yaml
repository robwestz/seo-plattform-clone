apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: seo-platform
  labels:
    app: redis
type: Opaque
stringData:
  password: "CHANGE-ME-IN-PRODUCTION"  # Use external secret manager in production
