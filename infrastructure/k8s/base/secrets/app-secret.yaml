apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: seo-platform
  labels:
    app: seo-platform
type: Opaque
stringData:
  jwt-secret: "CHANGE-ME-TO-A-LONG-RANDOM-STRING-IN-PRODUCTION"  # Use external secret manager in production
  encryption-key: "CHANGE-ME-IN-PRODUCTION"
  api-key: "CHANGE-ME-IN-PRODUCTION"
